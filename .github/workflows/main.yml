name: Main

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Deploy ACI
      id: deploy
      uses: alkue-com/acid@main
      with:
        action: deploy
        subscription: ${{ secrets.SUBSCRIPTION }}
        location: ${{ secrets.LOCATION }}
        rg: ${{ secrets.RG }}
        aci: ${{ secrets.ACI }}
        image: ghcr.io/alkue-com/runner:latest
        vnet: ${{ secrets.VNET }}
        subnet: ${{ secrets.SUBNET }}
        creds: ${{ secrets.AZURE_CREDENTIALS }}
        env_variables: LABELS=azure,dev NAME=acid
        env_secrets: REPOSITORY=${{ github.repository }}

    - name: Output subnet ACI was deployed to
      run: echo ${{ steps.deploy.outputs.subnet }}

    - name: Delete ACI
      id: delete
      uses: alkue-com/acid@main
      with:
        action: delete
        subscription: ${{ secrets.SUBSCRIPTION }}
        rg: ${{ secrets.RG }}
        aci: ${{ secrets.ACI }}
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Output subnet ACI was deleted from
      run: echo ${{ steps.delete.outputs.subnet }}
